#include <iostream>
#include <cmath>
#include <vector>

using namespace std;

vector<int> solution(int brown, int red)
{
    vector<int> answer;
    int w = red, h = 1;
    for (int h = 1; w >= h; ++h)
    {
        if ()
    }
    return answer;
}
int main()
{
    cout << solution();
    return 0;
}