#include <iostream>
#include <stack>
#include <string>

using namespace std;

int main()
{
    string str;
    int t;
    cin >> str;
    cin >> t;
    while (t--)
    {
        char cmd;
        cin >> cmd;

        if (cmd == 'P')
        {
            char p;
            cin >> p;
        }
    }
}